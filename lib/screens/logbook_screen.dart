import 'package:flutter/material.dart';

class LogbookScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Logbook'),
      ),
      body: Center(
        child: Text('Logbook Screen Content'),
      ),
    );
  }
}
